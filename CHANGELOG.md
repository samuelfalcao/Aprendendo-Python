# Changelog

All notable changes to this project will be documented in this file.

## v0.1.0 - 2025-12-14
- Corrige `Testes.py`: fixa importações, refatora `validar_campos`.
- Adiciona validação de `idade` (inteiro positivo) e `sexo` como `OptionMenu`.
- Refatora layout para `grid()` e adiciona placeholders.
- Extrai `validate_inputs()` com testes (`pytest`) — 6 testes passando.
- Adiciona `README.md` e `.gitignore`.
